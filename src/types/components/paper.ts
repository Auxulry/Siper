import {ReactNode} from 'react';

export declare namespace PaperDeclaration {
  interface PaperProps extends React.HTMLAttributes<HTMLDivElement> {
    className?: string;
    children?: ReactNode;
  }
}
